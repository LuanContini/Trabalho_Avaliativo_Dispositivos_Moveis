import { ScrollView, Text } from "react-native";

export default function NovoGastoScreen() {
    return (
        <ScrollView>
            <Text>Nova Tela de Gasto</Text>
        </ScrollView>
    );
}